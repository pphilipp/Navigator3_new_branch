<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:gravity="center_vertical|center_horizontal"
        android:padding="@dimen/button_padding"
        android:visibility="gone" >

        <Button
            android:id="@+id/btn_order"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/button_padding"
            android:background="@drawable/light_gradient_button"
            android:text="Заказы"
            android:textColor="@android:color/white" />

        <Button
            android:id="@+id/btn_busy"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/btn_order"
            android:layout_marginBottom="@dimen/button_padding"
            android:background="@drawable/light_gradient_button"
            android:text="Занят"
            android:textColor="@android:color/white" />

        <Button
            android:id="@+id/ethear_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/btn_busy"
            android:layout_marginBottom="@dimen/button_padding"
            android:background="@drawable/light_gradient_button"
            android:text="Эфир(0)"
            android:textColor="@android:color/white" />

        <Button
            android:id="@+id/test_test"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/ethear_button"
            android:background="@drawable/light_gradient_button"
            android:text="Дополнительно"
            android:textColor="@android:color/white" />
    </RelativeLayout>

    <ListView
        android:id="@+id/etherList"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:cacheColorHint="#00000000"
        android:dividerHeight="4dp" />

    <LinearLayout
        android:id="@+id/noEtherLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="3"
        android:orientation="vertical"
        android:visibility="gone" >

        <TextView
            android:id="@+id/noEther"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="center_horizontal"
            android:gravity="center_vertical"
            android:text="@string/ether"
            android:textColor="#B8000000"
            android:textSize="120sp"
            android:shadowColor="#38acaab4"
            android:shadowRadius="12" />

    </LinearLayout>

</LinearLayout>